(** {3 A Quadruplet List.} *)
(**
   [ (l, m, r); (l, m, r); ...; (l, m, r)]
 *)

    
type ('a, 'b, 'c, 'd) quadruplet_list = ('a, 'b, 'c, 'd) Quadruplet_t.quadruplet list
;;
(** It is a list of Quadruplet *)
