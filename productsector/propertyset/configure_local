# The sub-directories where we run a configure script.
SUB_DIRECTORIES="propertyset_formulas propertyset_interfaces propertyset_symbols propertyset_tags propertyset_values "

# The directory where we just conproperty .depend and .depend-rebuild.
CURRENT_AND_TEST=". unitary_tests"

# created by /code/sources/ocaml_top/setup/utilities/do_conproperty_local_for_any_set.sh > conproperty_local
