(** {3 Elementary_border_parameter_ecppef_coefficient_envelope_v} *)

(** {6 Documenting_for_envelope} *)

let documentation () = 
[
 "Current : VELE:Elementary_border_parameter_ecppef_coefficient_envelope_v";
 "Needs: VELE:Elementary_border_parameter_ecppef_coefficient_belt_v";
 "Needed-by: ";
 "What-is-it : a one element list of Envelopee i.e. (Elementary_border_tag, Readee)";
 "Remark : NO Sibling for any_envelope";
]
;;

let nam_cod = Management_v.current_module_name (documentation ());;

(** {6 Belt of Tag} *)

let belt_of_tag tag_eec =
  Elementary_border_parameter_ecppef_coefficient_belt_v.belt_of_tag tag_eec
;;

(** {6 Elementary_border_tag of Tag} *)

let elementary_border_tag_of_tag tag_eec =
  Tag_v.map_entity 
    Elementary_border_symbol_v.elementary_border_symbol_of_elementary_border_parameter_ecppef_coefficient_symbol 
    tag_eec
;;

(** {6 Readee of Tag} *)

let readee_of_tag tag_eec =
  Elementary_border_parameter_ecppef_coefficient_belt_v.readee_of_tag tag_eec
;;

(** {6 Envelopee of Tag} *)

let envelopee_of_tag tag_eec =
  let tag_ebo = elementary_border_tag_of_tag tag_eec in
  let rea = readee_of_tag tag_eec in
  Doublet_v.make tag_ebo rea
;;

(** {6 Envelope of Tag} *)

let envelope_of_tag tag_eec =
  let env= envelopee_of_tag tag_eec in
  [env]
;;

(** {6 Measure_actual of Tag} *)

let measure_actual_of_tag tag_eec =
  Elementary_border_parameter_ecppef_coefficient_belt_v.measure_actual_of_tag tag_eec
;;

let measure_actual_list_of_tag tag_eec =
  let mea = measure_actual_of_tag tag_eec in
  [mea]
;;

let measure_canonical_of_tag_of_calculation_name tag_eec =
  let mea = measure_actual_of_tag tag_eec in
  Measure_actual_v.measure_canonical_of_measure_actual_of_calculation_name mea
;;

let measure_canonical_list_of_tag tag_eec =
  let can_mea = measure_canonical_of_tag_of_calculation_name tag_eec in
  [can_mea]
;;

(** {6 Tagged_measure_actual} *)

let tagged_measure_actual_of_tag tag_eec =
  let mea_eec = measure_actual_of_tag tag_eec in 
  Doublet_v.make tag_eec mea_eec
;;

(** {6 Border as list of itself} *)

let coerce_up_tag tag_eec =
  Tag_v.map_entity 
    Elementary_border_symbol_v.elementary_border_symbol_of_elementary_border_parameter_ecppef_coefficient_symbol 
    tag_eec
;;

let border_of_tag tag_eec =
  [coerce_up_tag tag_eec]
;;

(** {6 Envelopee off Envelope} *)

let envelopee_off_envelope env_eec =
  try List_v.element_off_one_element_list env_eec 
  with Failure _ -> 
    Error_messages_v.print_fatal_error __LOC__ "envelopee_off_envelope"
      "One element list"
      (Format.sprintf "Envelopee list is :@.   %s" (List_v.name Envelopee_v.fullname env_eec))
      "Check"
;;

(** {6 Elementary_border_tag off Envelope} *)

let elementary_border_tag_off_envelope env_eec =
  let env = envelopee_off_envelope env_eec in
  Doublet_v.left_off_doublet env
;;

(** {6 Tagged_readee} *)

let tagged_readee_of_tag tag_eec =
  let rea_eec = readee_of_tag tag_eec in
  Doublet_v.make tag_eec rea_eec
;;

(** {6 Float} *)

let float_of_tag tag_eec  =
  let mea = measure_actual_of_tag tag_eec in
  Measure_actual_v.float_off_measure_actual mea 
;;

(** {6 Units} *)

let elementary_fence_units_actual_tag_of_tag tag_eec =
  let mea = measure_actual_of_tag tag_eec in
  Measure_actual_v.elementary_fence_units_actual_symbol_off_measure_actual mea
;;

(** {6 Text} *)

let text_of_tag tag_eec =
  Elementary_border_parameter_ecppef_coefficient_belt_v.text_of_tag tag_eec
;;

(** {6 Readee off Envelope} *)

let readee_off_envelope env_eec =
  let env = envelopee_off_envelope env_eec in
  Envelopee_v.readee_off_envelopee env
;;

(** {6 Measure_actual off Envelope} *)

let measure_actual_off_envelope env_eec =
  let env = envelopee_off_envelope env_eec in
  Envelopee_v.measure_actual_off_envelopee env
;;

let measure_actual_list_off_envelope env_eec =
  let mea = measure_actual_off_envelope env_eec in
  [mea]
;;

let measure_canonical_off_envelope env_eec =
  let mea = measure_actual_off_envelope env_eec in
  Measure_actual_v.measure_canonical_of_measure_actual_of_calculation_name mea
;;

let measure_canonical_list_off_envelope env_eec =
  let can_mea = measure_canonical_off_envelope env_eec in
  [can_mea]
;;

(** {6 Closure : list of Readee} *)

let closure_off_envelope env_eec =
  let rea = readee_off_envelope env_eec in
  [rea]
;;

let closure_of_tag tag_eec =
  let rea = readee_of_tag tag_eec in
  [rea]
;;

(** {6 Naming_for_envelope} *)

let name env_eec =
  List_v.name Envelopee_v.name env_eec
;;

let fullname env_eec =
  List_v.name Envelopee_v.fullname env_eec
;;

let longname env_eec =
  List_v.name Envelopee_v.longname env_eec
;;

(* generated by ./do_envelope_as_list_border_trunc_units_v.sh elementary_border_parameter_ecppef_coefficient eec elementary_fence_units_actual *)
(* using ./template_envelope_as_list_border_trunc_units_v.ml *)
(* Done on mardi 24 mai 2016, 17:35:56 (UTC+0200) *)

