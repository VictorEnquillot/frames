(** {3 Elementary_border_coordinate_heterogeneous_spherical_phi_belt_v} *)

(** {6 Documenting_for_belt} *)

let documentation () = 
  [
   "Current : VELE:Elementary_border_coordinate_heterogeneous_spherical_phi_belt_v";
   "Needs : TELE:Thup_tag_v";
   "Needs : VELE:Elementary_border_coordinate_heterogeneous_spherical_phi_container_v";
   "Needed-by: ";
   "What-is-it : it is a belt of a Bud Type as a Doublet of its own Tag and a Measure";
   "Remark : no Bridge in Elementary Domain";
 ]
;;

let nam_mod = Management_v.current_module_name (documentation ());;

(** {6 Coercing Up Tag} *)

let coerce_up_tag tag_esp =
  Tag_v.map_entity 
    Elementary_border_symbol_v.elementary_border_symbol_of_elementary_border_coordinate_heterogeneous_spherical_phi_symbol 
    tag_esp
;;

(** {6 Border as list of itself} *)

let border_of_tag tag_esp =
  [coerce_up_tag tag_esp]
;;

(** {6 Extraction from Database : Measure_actual} *)

let measure_actual_angle_of_tag tag_esp =
  let tag_faa = 
    Elementary_border_coordinate_heterogeneous_spherical_phi_container_v.container_of_tag 
      tag_esp
  in
  Elementary_fence_units_actual_angle_belt_v.measure_actual_angle_of_tag tag_faa
;;

(** {6 Measure_actual} *)

let measure_actual_of_tag tag_esp =
  let msr = measure_actual_angle_of_tag tag_esp in
  Measure_actual_angle_v.measure_actual_of_measure_actual_angle msr
;;

let measure_canonical_of_tag_of_calculation_name tag_esp nam_cal =
  let mea = measure_actual_of_tag tag_esp in
  Measure_actual_v.measure_canonical_of_measure_actual_of_calculation_name mea nam_cal
;;

(** {6 Belt} *)

let belt_of_tag tag_esp =
  let msr = measure_actual_angle_of_tag tag_esp in
  Doublet_v.make tag_esp msr
;;

(** {6 Extracting Measure_actual off Belt} *)

let measure_actual_angle_off_belt bel_esp =
  Doublet_v.right_off_doublet bel_esp
;;

let measure_actual_off_belt bel_esp =
  let msr = measure_actual_angle_off_belt bel_esp in
  Measure_actual_angle_v.measure_actual_of_measure_actual_angle msr
;;

let measure_actual_list_off_belt bel_esp =
  let mea = measure_actual_off_belt bel_esp in
  [mea]
;;

let measure_canonical_off_belt_of_calculation_name bel_esp nam_cal =
  let mea = measure_actual_off_belt bel_esp in
  Measure_actual_v.measure_canonical_of_measure_actual_of_calculation_name mea nam_cal
;;

let measure_canonical_list_off_belt_of_calculation_name bel_esp nam_cal =
  let can_mea = measure_canonical_off_belt_of_calculation_name bel_esp nam_cal in
  [can_mea]
;;

(** {6 Readee : a Generalization of Measure_actual} *)

let readee_off_belt bel_esp =
  let mea = measure_actual_off_belt bel_esp in
  Readee_v.readee_of_measure_actual mea
;;

let readee_of_tag tag_esp =
  let bel_esp = belt_of_tag tag_esp in
  readee_off_belt bel_esp 
;;

(** {6 Closure : list of Readee} *)

let closure_off_belt bel_esp =
  let rea = readee_off_belt bel_esp in
  [rea]
;;

let closure_of_tag tag_esp =
  let bel_esp = belt_of_tag tag_esp in
  closure_off_belt bel_esp
;;

(** {6 Float} *)

let float_of_tag tag_esp  =
  let man = measure_actual_angle_of_tag tag_esp in
  Measure_actual_angle_v.float_off_measure_actual_angle man 
;;

(** {6 Elementary_fence_units_actual} *)

let elementary_fence_units_actual_tag_of_tag tag_esp =
  let man = measure_actual_angle_of_tag tag_esp in
  Measure_actual_angle_v.elementary_fence_units_actual_angle_symbol_off_measure_actual_angle man
;;

(** {6 Text} *)

let text_of_tag tag_esp =
  let (flo, sym_esp) = measure_actual_angle_of_tag tag_esp in
  Format.sprintf "angle %f %s" flo ( Elementary_fence_units_actual_angle_symbol_v.name sym_esp)
;;

(** {6 Naming_for_belt} *)

let name bel_esp =
  Doublet_v.name 
    Elementary_border_coordinate_heterogeneous_spherical_phi_tag_v.name
    Measure_actual_angle_v.name
    bel_esp
;;

let longname bel_esp =
  Format.sprintf "Elementary_border_coordinate_heterogeneous_spherical_phi_belt_t.%s" (String.capitalize (name bel_esp))
;;


(* generated by ./do_belt_as_doublet_border_bud_1sibling_1units_2beltee_v.sh elementary_border_coordinate_heterogeneous_spherical ehs phi esp elementary_fence_units_actual efa angle faa man *)
(* using ./template_belt_as_doublet_border_bud_1sibling_1units_2beltee_v.ml *)
(* Done on mardi 24 mai 2016, 16:43:10 (UTC+0200) *)

